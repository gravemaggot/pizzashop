<h2>Ваш заказ:</h2>
<div class="bs-example" data-example-id="contextual-table">
    <table class="table">
      <thead>
        <tr>
          <th>#</th>
          <th>Пицца</th>
          <th>Количество</th>
        </tr>
      </thead>
      <tbody>
        <% num = 0; @results.each do |row| %>
        <% num = num + 1; %>
          <tr>
            <th scope="row"><%= num %></th>
              <td><%= row[:name] %></td>
              <td><%= row[:cnt]    %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
</div>
<form action="/submit_cart" method="post">
  <div class="form-group">
    <label>Ваше имя</label>
    <input name = "order[username]" type="text" class="form-control" placeholder="Введите ваше имя" value = "<%= @new_order.username %>">
  </div>
  <div class="form-group">
    <label>Ваш телефон</label>
    <input id = "phone" name = "order[phone]" type="text" class="form-control" placeholder="+375 (99) 999-99-99" value = "<%= @new_order.phone %>">
  </div>
  <div class="form-group">
    <label>Адрес доставки</label>
    <input name = "order[adres]" type="text" class="form-control" placeholder="Введите адрес доставки" value = "<%= @new_order.adres %>">
  </div>
	<input id="orders_input" name="order[orders]" type="hidden" value=<%= @pizzas %> />
	<input id="orders_button" type="submit" class="btn btn-primary btn-lg" value="Заказать" />
</form>


<script type="text/javascript">

$(function () {
    update_orders_button();
})

</script>
